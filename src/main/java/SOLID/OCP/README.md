开闭原则（Open-Closed Principle, OCP）是面向对象设计的基本原则之一。这个原则指出，软件实体（比如类、模块、函数等）应该对扩展开放，但对修改封闭。这意味着，一个实体允许其行为扩展，而不需要修改其源代码。

开闭原则的核心思想是鼓励使用抽象构建框架，而将具体的实现延迟到客户端来完成。这样，新增功能时，可以通过添加新的代码而不是修改已有代码，从而减少对现有功能的风险。

为了说明开闭原则，我们可以考虑一个简单的图形绘制应用。在不使用开闭原则的情况下，每次添加新的图形类型，都需要修改绘图逻辑。但如果遵循开闭原则，我们可以设计一个更灵活的架构。
